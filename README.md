# Confectionery API 🍰

Web API для автоматизації бізнес-процесів кондитерського підприємства. 
Проєкт розроблено в рамках курсової роботи з дисципліни «Компоненти програмної інженерії».

**Варіант:** 19 (Непарний)

**Автор:** Студентка групи КП-33 Храполович Вікторія

## Опис

Система надає RESTful API для управління каталогом кондитерських виробів, ведення бази клієнтів та обробки замовлень. 
Архітектура побудована за N-рівневим принципом із чітким виділенням бізнес-логіки.

**Основні можливості:**
* **Каталог виробів:** CRUD-операції (створення, читання, оновлення, видалення) для кондитерських виробів.
* **Клієнти:** Реєстрація та управління даними клієнтів.
* **Замовлення:** Оформлення замовлень з автоматичною валідацією бізнес-правил (ліміти кількості, перевірка наявності).
* **Бізнес-логіка:** Автоматичне визначення статусу замовлення («Нове» / «Обробляється») залежно від обсягу.

## Технології

У проєкті використано сучасний стек технологій екосистеми Microsoft:

* **Платформа:** .NET 9 (ASP.NET Core Web API)
* **Мова:** C# 12
* **База даних:** PostgreSQL
* **ORM:** Entity Framework Core (Code First)
* **Валідація:** FluentValidation
* **Тестування:** xUnit, Moq
* **Документація:** Swagger / OpenAPI

## Як запустити проєкт

### Попередні вимоги
* .NET 9 SDK
* PostgreSQL Server

### Кроки для запуску

1.  **Клонуйте репозиторій:**
    ```bash
    git clone [https://github.com/vika251/coursework_kpi.git](https://github.com/vika251/coursework_kpi.git)
    cd coursework_kpi
    ```

2.  **Налаштуйте базу даних:**
    Відкрийте `appsettings.json` та перевірте рядок підключення `ConnectionStrings:DefaultConnection`. Переконайтеся, що ваші дані (Host, Port, Username, Password) є коректними.

3.  **Застосуйте міграції:**
    Виконайте команду для створення бази даних та таблиць:
    ```bash
    dotnet ef database update
    ```

4.  **Запустіть проєкт:**
    ```bash
    dotnet run --project coursework_kpi
    ```
    Після запуску API буде доступне за адресою: `http://localhost:5201/swagger` (або інший порт, вказаний у консолі).

## Тестування

Проєкт має високий рівень покриття бізнес-логіки модульними тестами (Unit Tests).

**Щоб запустити тести, виконайте команду:**
```bash
dotnet test